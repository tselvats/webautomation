# webautomation

This framework is built using Java, Selenium and TestNG framework. 
To run the test, download the corresponding chromedriver to your machine, and place it in src\main\resources\drivers path and run the testng.xml in the main project folder.
